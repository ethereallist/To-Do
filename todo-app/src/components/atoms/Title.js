import { styled } from "styled-components"

const StyledTitle = styled.h1`

`

function Title() {
    return (
        <div>
            <StyledTitle>
            Create free account
            </StyledTitle>
        </div>
    )
}

export default Title